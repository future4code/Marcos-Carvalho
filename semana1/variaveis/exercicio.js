//exercico1

const nome 
const idade 
typeof nome
typeof idade
//como não atribui nenhum valor  aparece undefined, pois não tem atribuição
const nome = prompt("Qual é o seu nome?")
const idade = prompt("Qual sua idade?")
const nome = "Marcos"
const idade = 23

console.log  ("Olá meu nomé", nome, "eu tenho", idade,"anos")
 typeof nome
 typeof idade

 //Agora aparece o tipo de cada, pois com "volta como string" e o idade vai voltar como Number

//exercico2


////const roupa = prompt("voce esta usando roupa azul hoje?")
//const roupa = prompt("voce esta feliz hoje?")
const roupa = prompt("voce esta com fome hoje?")
n = sim
m = não 
t = 'não interessa!'

console.log("voce esta usando roupa azul hoje?",n,"voce esta feliz hoje?",m,"voce esta com fome hoje?",t,)

//exericcio3
let a = 10
let b = 25

// Aqui faremos uma lógica para trocar os valores

// Depois de trocados, teremos o seguinte resultado:
console.log("O novo valor de a é", a) // O novo valor de a é 25
console.log("O novo valor de b é", b) // O novo valor de b é 10
a = b
b = a
c = a

///Desafio
//1. O primeiro número somado ao segundo número resulta em: x.
//2. O primeiro número multiplicado pelo segundo número resulta em: y.




































